export const metadata = {
  title: "Sanity Studio",
  description: "Content management for Court Garr's blog",
};

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
